<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>SantaChat Access | Magic Mail Co.</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Simple on-page styling -->
  <style>
    :root {
      --bg: #faf5f0;
      --card: #ffffff;
      --accent: #c0392b;
      --accent-soft: #f5d3cb;
      --text-main: #2c3e50;
      --text-soft: #6c757d;
      --shadow-soft: 0 14px 30px rgba(0, 0, 0, 0.08);
      --radius-lg: 18px;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
        sans-serif;
      background: radial-gradient(circle at top, #ffe9dd, #faf5f0 60%);
      color: var(--text-main);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 24px;
    }

    .page {
      width: 100%;
      max-width: 900px;
      background: transparent;
    }

    .logo-area {
      text-align: center;
      margin-bottom: 24px;
    }

    .logo-text {
      font-size: 1.4rem;
      font-weight: 600;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: var(--text-soft);
    }

    .card {
      display: grid;
      grid-template-columns: minmax(0, 3fr) minmax(0, 2.2fr);
      gap: 32px;
      background: var(--card);
      border-radius: var(--radius-lg);
      box-shadow: var(--shadow-soft);
      padding: 32px;
      border: 1px solid rgba(0, 0, 0, 0.03);
    }

    @media (max-width: 800px) {
      .card {
        grid-template-columns: 1fr;
        padding: 24px;
      }
    }

    .left h1 {
      font-size: 2rem;
      margin-bottom: 8px;
    }

    .subtitle {
      font-size: 1rem;
      color: var(--text-soft);
      margin-bottom: 20px;
    }

    .highlight {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: var(--accent-soft);
      color: var(--accent);
      padding: 6px 12px;
      border-radius: 999px;
      font-size: 0.85rem;
      font-weight: 500;
      margin-bottom: 16px;
    }

    .highlight-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: var(--accent);
    }

    ul.feature-list {
      list-style: none;
      margin: 0 0 24px 0;
      padding: 0;
    }

    .feature-list li {
      display: flex;
      align-items: flex-start;
      gap: 10px;
      margin-bottom: 10px;
      font-size: 0.95rem;
      color: var(--text-main);
    }

    .feature-bullet {
      width: 9px;
      height: 9px;
      border-radius: 3px;
      background: var(--accent-soft);
      margin-top: 6px;
    }

    .fine-print {
      font-size: 0.8rem;
      color: var(--text-soft);
      line-height: 1.4;
    }

    .right {
      border-left: 1px solid rgba(0, 0, 0, 0.04);
      padding-left: 24px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      gap: 18px;
    }

    @media (max-width: 800px) {
      .right {
        border-left: none;
        border-top: 1px solid rgba(0, 0, 0, 0.06);
        padding-left: 0;
        padding-top: 20px;
      }
    }

    .price-row {
      display: flex;
      align-items: baseline;
      gap: 10px;
    }

    .price-main {
      font-size: 1.8rem;
      font-weight: 700;
      color: var(--accent);
    }

    .price-note {
      font-size: 0.85rem;
      color: var(--text-soft);
    }

    .tagline {
      font-size: 0.9rem;
      color: var(--text-main);
      margin-top: 6px;
    }

    .cta-footer {
      display: flex;
      flex-direction: column;
      gap: 10px;
      margin-top: 10px;
    }

    .primary-checkout-button {
      width: 100%;
      border: none;
      border-radius: 999px;
      padding: 14px 18px;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      background: linear-gradient(135deg, #c0392b, #e67e22);
      color: #fff;
      box-shadow: 0 10px 20px rgba(192, 57, 43, 0.35);
      transition: transform 0.08s ease, box-shadow 0.08s ease,
        filter 0.08s ease;
    }

    .primary-checkout-button:hover {
      transform: translateY(-1px);
      box-shadow: 0 14px 26px rgba(192, 57, 43, 0.45);
      filter: brightness(1.03);
    }

    .primary-checkout-button:active {
      transform: translateY(0);
      box-shadow: 0 6px 12px rgba(192, 57, 43, 0.35);
      filter: brightness(0.97);
    }

    .guarantee {
      font-size: 0.8rem;
      color: var(--text-soft);
      text-align: center;
    }

    .badge-row {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 10px;
      margin-top: 8px;
      font-size: 0.75rem;
      color: var(--text-soft);
      flex-wrap: wrap;
    }

    .pill {
      padding: 4px 10px;
      border-radius: 999px;
      border: 1px solid rgba(0, 0, 0, 0.06);
      background: #fafafa;
    }
  </style>
</head>
<body>
  <div class="page">
    <div class="logo-area">
      <div class="logo-text">Magic Mail Co.</div>
    </div>

    <main class="card">
      <section class="left">
        <div class="highlight">
          <span class="highlight-dot"></span>
          <span>The Official Connection to Santa&#39;s Workshop</span>
        </div>

        <h1>Unlock Your Family&#39;s Private SantaChat Room</h1>
        <p class="subtitle">
          A secure, parents-only space where every message to Santa feels
          magical, personal, and perfectly on-brand for your family.
        </p>

        <ul class="feature-list">
          <li>
            <span class="feature-bullet"></span>
            <span>Beautiful, ad-free chat experience designed for Christmas magic.</span>
          </li>
          <li>
            <span class="feature-bullet"></span>
            <span>Parents type the replies, Santa delivers them in his voice.</span>
          </li>
          <li>
            <span class="feature-bullet"></span>
            <span>Perfect for wish lists, behavior boosts, and Christmas countdowns.</span>
          </li>
          <li>
            <span class="feature-bullet"></span>
            <span>Instant access after checkout – no apps to install.</span>
          </li>
        </ul>

        <p class="fine-print">
          By completing your purchase, you&#39;ll receive a secure access link and
          any password details on the confirmation page and by email. Test
          payments use Stripe&#39;s sandbox and will not charge your card.
        </p>
      </section>

      <aside class="right">
        <div>
          <div class="price-row">
            <div class="price-main">$X</div>
            <div class="price-note">one-time family access</div>
          </div>
          <p class="tagline">
            Your private SantaChat room for this Christmas season.
          </p>
        </div>

        <div class="cta-footer">
          <button id="checkout-button" class="primary-checkout-button">
            Get Access to SantaChat
          </button>

          <p class="guarantee">
            Secure checkout powered by Stripe. You&#39;ll be redirected to
            Stripe to complete your purchase.
          </p>

          <div class="badge-row">
            <span class="pill">SSL Secured</span>
            <span class="pill">Stripe Checkout</span>
            <span class="pill">Parent-Only Access</span>
          </div>
        </div>
      </aside>
    </main>
  </div>

  <!-- Stripe + Checkout Script -->
  <script src="https://js.stripe.com/v3"></script>
  <script>
    // 1) Your Stripe PUBLISHABLE key (safe for frontend)
    //    Replace this with your actual pk_test_... key.
    const stripe = Stripe("pk_live_51S4uQSANoGJslfY6txmnOYjDQdGIy4JPPStSuF63itPN1ajXrJzFMJvDcWIVzkc6hE41O4z3ARoaIAIPcmIMYdQR00yu7se3SV");

    const checkoutButton = document.getElementById("checkout-button");

    checkoutButton.addEventListener("click", async () => {
      try {
        // 2) Call your backend on Render to create a Checkout Session
        const response = await fetch(
          "https://santachat-stripe-backend.onrender.com/create-checkout-session",
          {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({})
          }
        );

        if (!response.ok) {
          alert("Unable to start checkout. Please try again in a moment.");
          return;
        }

        const data = await response.json();

        // 3) Redirect to Stripe-hosted checkout
        const result = await stripe.redirectToCheckout({
          sessionId: data.id
        });

        if (result.error) {
          alert(result.error.message || "Something went wrong with checkout.");
        }
      } catch (err) {
        console.error(err);
        alert("Something went wrong. Please try again.");
      }
    });
  </script>
</body>
</html>
